# Telegram bots in one process (no server)

Проект запускает 2 Telegram-бота в одном `main.py` через long polling:

- `sender` (админ-бот): админ-панель, управление пользователями и рассылками.
- `receiver` (публичный бот): умный калькулятор + регистрация + онлайн-дуэль.

## Receiver-бот

Логика:

1. `/start` запускает регистрацию (ввести имя).
2. Имя должно быть уникальным (дубликаты запрещены).
3. Пока регистрация не завершена, вычисления недоступны.
4. Пока регистрация не завершена, нижняя клавиатура скрыта.
5. После регистрации появляются кнопки:
   - `🧮 Начать считать`
   - `❓ Помощь`
   - `📊 Статистика`
   - `⚙️ Настройки`
   - `🎮 Онлайн-игра`
   - `👥 Друзья`
   - `🏠 Домой`

Команды:

- `/start` - регистрация/вход
- `/help` или `help` - помощь по формулам
- `/privacy` - данные текущего пользователя
- `/stop` - сброс регистрации

Калькулятор поддерживает:

- знаки: `+ - * / : × ÷ ^`
- функции: `sin cos tan asin acos atan sqrt abs ln log log10 exp round floor ceil fact`
- уравнения с `x`, например `x^2-5*x+6=0`
- формы ввода: `2(3+4)`, `2х3`, `2x3`, `10:2`

Настройки receiver:

- изменить имя регистрации
- выбрать класс/уровень: `1-4`, `5-8`, `9-11`, `pro`
- включить/выключить входящие заявки в друзья
- включить/выключить поиск по имени регистрации

Персональная статистика receiver:

- посещения и последний вход
- статистика вычислений
- статистика онлайн-игры (W/L/D, очки)
- количество друзей

Друзья:

- отправка заявки по `@username` или по имени регистрации (если у пользователя разрешен поиск по имени)
- входящие заявки: принять/отклонить
- список друзей
- карточка друга + удаление из друзей
- приглашение друга в онлайн-дуэль по кнопке

Онлайн-игра:

- поиск соперника (очередь)
- когда соперник найден: правила + кнопка `START` у обоих
- обратный отсчет `3..2..1`
- дуэль на `5` раундов (кто первый ответил правильно, получает очко)
- в раунде показывается, кто какой ответ отправил (`верно/неверно`)
- сообщения с ответами и прошлый пример очищаются перед новым раундом
- в раунде можно нажать `🚩 Пожаловаться` и отправить жалобу на соперника
- результаты сохраняются в статистику

## Sender-бот (админ)

Вход:

1. `/start`
2. пароль 3 раза подряд

Пароль в `.env`:

- `ADMIN_PASSWORD=151214`

Команды:

- `/panel`, `/status` - панель
- `/users` - список пользователей с кнопками
- `/users all` - текстовый список
- `/user <id|@username>` - карточка
- `/user_regs <id|@username>` - история регистраций выбранного пользователя
- `/ban <id|@username>`, `/unban <id|@username>`
- `/mute <id|@username>`, `/unmute <id|@username>`
- `/remove <id|@username>`
- `/send <id|@username> <текст>`
- `/setname <id|@username> <имя>` - сменить имя регистрации
- `/friend_add <id|@u> <id|@u>` - принудительно добавить дружбу
- `/friend_remove <id|@u> <id|@u>` - удалить дружбу
- `/ban_for <id|@u> <время>` - временный бан (`1h`, `12h`, `1d`, `12:00`, `2026-02-20 18:30`)
- `/broadcast <текст>`
- `/broadcast_to <id|@u,id|@u> <текст>`
- `/top` - топ активных
- `/reports` - открытые жалобы
- `/registrations` - журнал регистраций
- `/autoclean <off|10m|1h|12h|1d>` - автоочистка админ-чата (по умолчанию выключена)
- `/cancel`
- `/help`

Кнопки карточки пользователя:

- `Данные`
- `Статистика`
- `Вкл/Выкл рассылку`
- `Бан/Разбан`
- `Написать`
- `Удалить`
- `Сменить имя`
- `Добавить друга`
- `Бан на время`
- `Регистрации`

Новые окна в админке:

- `Жалобы` - отдельный список жалоб от игроков с карточкой жалобы и кнопкой закрытия
- `Регистрации` - отдельный журнал: кто зарегистрировался и когда
- `Автоочистка` - отдельная настройка автоудаления сообщений админ-бота (выключено по умолчанию)

## Файлы

- `main.py` - логика
- `.env` - токены и пароль админа
- `start.sh` - старт в фоне
- `stop.sh` - остановка
- `status.sh` - статус
- `bot_state.sqlite3` - база данных

## Запуск

```bash
cd /Users/pakhomenka/Documents/игра
python3 main.py
```

## Деплой 24/7 (бесплатно) на Render

В проект уже добавлено:

- `render.yaml` (конфиг сервиса)
- встроенный HTTP health (`/health`) в `main.py`
- self-ping режим (для бесплатного web-инстанса)

Что нужно сделать:

1. Залить проект в GitHub.
2. В Render создать сервис из этого репозитория (Blueprint или New Web Service).
3. В переменные окружения в Render добавить:
   - `SENDER_BOT_TOKEN`
   - `RECEIVER_BOT_TOKEN`
   - `ADMIN_PASSWORD` (например `151214`)
4. Убедиться, что команда старта: `python3 main.py`.
5. После первого деплоя сервис начнет long polling Telegram.

Технически:

- Если в окружении есть `PORT`, бот поднимет мини HTTP-сервер на `0.0.0.0:$PORT`.
- Для anti-sleep используется автопинг:
  - `KEEPALIVE_URL` (если задан вручную),
  - иначе `RENDER_EXTERNAL_URL` (подхватывается автоматически на Render).
- Интервал пинга задается `KEEPALIVE_INTERVAL_SECONDS` (по умолчанию `480`).

Важно:

- База `bot_state.sqlite3` хранится в файловой системе контейнера.
- На бесплатных тарифах при рестарте/пересоздании инстанса локальные файлы могут потеряться.
