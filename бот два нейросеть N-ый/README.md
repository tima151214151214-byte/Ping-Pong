# Православие простым языком (Telegram Bot)

Проект находится в папке `бот два нейросеть N-ый`.

## Порядок запуска для пользователя

1. Если в `.env` задан серверный API, бот подставляет его автоматически.
2. Пользователь может в любой момент задать **личный IP/API** командой `/connect`.
3. После регистрации доступны ответы.

## Команда подключения IP/API

```text
/connect <BASE_URL/IP> <API_KEY> [MODEL]
/connect <API_KEY>
/connect <BASE_URL/IP>
```

Примеры:

```text
/connect https://openrouter.ai/api/v1 sk-or-xxx openrouter/free
/connect http://192.168.1.20:11434/v1 ollama qwen2.5:7b
```

Короткий формат автоматически подставляет недостающие параметры из `.env`.

## Что умеет бот

- Регистрация при первом `/start`.
- Меню с кнопками:
  - `❓ Спросить вопрос`
  - `📊 Осталось запросов`
  - `⚙️ Настройки`
  - `🔌 Как вставить свой API`
- Отвечает только по теме Библии, Бога и христианского учения.
- Коротковременная память: хранит последние несколько пар `вопрос-ответ`, поэтому понимает уточнения вроде «а почему так?».
- Всегда отвечает на русском.
- 4 параллельные модели + итоговая сверка.
- Живой прогресс-бар 0..100% в одном редактируемом сообщении.

## Настройки

- Конфессия: `Православный` / `Католик`.
- Длина ответа: от очень короткого до очень длинного.
- Как объяснять: `По-православному`, `Простым языком`, `Сложно → легко`.
- Режим размышления: `Быстро`, `Стандарт`, `Глубоко`.
- Пресет модели: ограниченный список, чтобы не «орать токенами».

## Запуск

```bash
cd "/Users/pakhomenka/Documents/Telegram бот/бот два нейросеть N-ый"
python3 -m pip install -r requirements.txt
python3 main.py
```
